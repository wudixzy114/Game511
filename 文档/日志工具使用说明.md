# 日志工具使用说明

## 日志文件

日志目录：

```text
logs/
```

每类日志只保留当前和上一次两份：

- `application.log`
- `application.log.1`
- `error.log`
- `error.log.1`
- `performance.log`
- `performance.log.1`

程序每次启动时会先轮转旧日志：

- 当前 `*.log` 移动为 `*.log.1`
- 旧的 `*.log.1` 被删除
- 新运行写入新的 `*.log`

## HTML 查看器

生成日志查看页面：

```powershell
.\scripts\log.ps1
```

默认输出：

```text
logs/log-report.html
```

指定目录或输出：

```powershell
.\scripts\log.ps1 -LogDir .\logs -Output .\logs\log-report.html
```

HTML 页面支持过滤：

- 日志类型：application / error / performance
- 日志级别：ERROR / WARN / INFO / DEBUG / TRACE
- 系统 target：例如 `dao_game::world::streaming`
- 全文搜索：message、fields、target、source

## 格式

新日志统一写为 JSON 行，便于工具解析和过滤。旧文本日志仍会作为 raw 行展示，但 target 和字段过滤能力会弱一些。
