# 《道》任务清单：大型开放世界地形流式生成（2026-05-11）

## 阶段目标

1. 将程序化地形从启动时整图预生成升级为按需采样、按可见范围生成的开放世界地形底座。
2. 保持地形、征兆、展示模式和第一人称探索的统一采样接口，为后续生态、天气和 AI 历险提供更大空间载体。

## 任务项

### 1. 地形数据层

- [x] 将 `WorldMap` 改为保存种子和地形规则，不再缓存整张世界的顶点和地块数组。
- [x] 支持任意世界坐标的高度、湿度、坡度、河流、侵蚀和群系确定性采样。
- [x] 增加大型世界下的有限逻辑边界和展示锚点搜索半径。

### 2. 分块流式生成

- [x] 将可见分块坐标改为基于相机世界坐标的有符号分块。
- [x] 按需为分块生成连续网格，边界共享同一套世界坐标采样规则。
- [x] 将地表细节物改为随分块加载和卸载。

### 3. 验证与文档

- [x] 更新默认配置，使世界半径达到大型开放世界原型尺度。
- [x] 补充测试覆盖远距离采样、分块坐标和分块网格生成。
- [x] 更新系统设计和工具说明，记录新的地形流式能力。

## 完成记录

- `WorldMap` 已从整图顶点缓存改为种子与规则驱动的按需采样。
- 地形网格和地表细节物已跟随相机可见分块加载与卸载，分块坐标支持世界原点两侧的大范围移动。
- 默认配置已扩展为 `world_radius = 512`、`chunk_radius = 6`、`visible_chunk_radius = 4`、`showcase_search_radius = 48`。
- 已通过 `scripts/dev-check.ps1` 和 `DAO_AUTO_EXIT_SECONDS=1.0 cargo run` 验证。
