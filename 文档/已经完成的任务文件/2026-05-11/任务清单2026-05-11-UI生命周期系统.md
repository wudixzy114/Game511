# 《道》任务清单：UI 生命周期系统（2026-05-11）

## 最终目标

1. 建立游戏级 UI 与生命周期状态机。
2. 打通主菜单、进入游戏、暂停、继续、重开、返回主菜单、退出程序的完整流程。
3. 保留展示模式兼容路径，并为后续设置页、存档页、叙事 HUD 扩展留下清晰边界。

## 主要任务1

### 1. 应用状态机

- [x] 阅读 `AI开发流程.md`、游戏介绍和质量要求。
- [x] 新增应用级 `AppScreen` 状态，区分主菜单和游戏会话。
- [x] 新增 `InGameState` 子状态，区分运行中和暂停。
- [x] 新增 `SessionMode`，统一管理探索模式与展示模式。
- [x] 保留 `DAO_PRESENTATION_MODE=1` 直进展示模式的脚本兼容能力。

### 2. UI 界面

- [x] 新增 Bevy UI 主菜单。
- [x] 新增游戏 HUD，显示当前模式、时间、地貌、坐标、共鸣、平静和征兆。
- [x] 新增暂停菜单，支持继续、重新开始、返回主菜单和退出。
- [x] 接通按钮点击行为，使用 UI 完成主要游戏生命周期控制。

## 主要任务2

### 1. 生命周期接入

- [x] 世界系统改为进入游戏时生成，离开游戏时清理资源。
- [x] 环境系统改为随游戏会话初始化和清理。
- [x] 征兆系统改为随游戏会话初始化和运行。
- [x] 玩家控制改为只在探索模式、运行状态下启用。
- [x] 展示导演改为只在展示模式、运行状态下启用。
- [x] 暂停时释放鼠标，恢复游戏时重新锁定鼠标。

### 2. 质量与验证

- [x] 运行 `cargo fmt --all`。
- [x] 运行 `cargo check`。
- [x] 运行 `cargo test`，42 个测试通过。
- [x] 运行 `cargo clippy --all-targets -- -D warnings`。
- [x] 使用 `DAO_AUTO_EXIT_SECONDS=3 cargo run` 验证主菜单启动路径。
- [x] 使用 `DAO_PRESENTATION_MODE=1 DAO_AUTO_EXIT_SECONDS=4 cargo run` 验证展示模式直进路径。

## 任务额外说明

本阶段完成的是 UI 与生命周期底座，不是最终美术 UI。后续可以在当前结构上继续扩展设置页、存档页、键位配置、加载界面和更具沉浸感的征兆表现。
