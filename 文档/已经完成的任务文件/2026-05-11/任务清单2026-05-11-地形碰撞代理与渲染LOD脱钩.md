# 《道》任务清单：地形碰撞代理与渲染 LOD 脱钩（2026-05-11）

## 阶段目标

1. 为近景地表建立独立的碰撞代理层，不再让角色落地采样依赖当前渲染分块状态。
2. 将碰撞采样与渲染 LOD 分块彻底分离，为后续物理碰撞体和远景层扩展打底。
3. 保持大型开放世界流式链路下的稳定性和可验证性。

## 任务项

### 1. 碰撞代理层

- [x] 新增独立的地形碰撞代理配置、缓存和近景目标半径。
- [x] 以固定高精度细分生成碰撞代理分块，不复用渲染 LOD 网格实体。
- [x] 建立碰撞代理分块的按位置更新、预算构建和缓存淘汰逻辑。

### 2. 采样接入

- [x] 玩家移动改用碰撞代理采样地面高度。
- [x] 漫游体地面跟随改用碰撞代理采样。
- [x] 保留 `WorldMap` 直接采样回退，避免代理分块未就绪时出现空窗。

### 3. 验证与文档

- [x] 补充测试覆盖碰撞代理目标选择、缓存采样和回退逻辑。
- [x] 更新系统设计文档与开发工具说明。
- [x] 运行格式化、编译、测试、clippy 和启动烟雾验证。

## 完成记录

- 新增 `TerrainCollisionProxy`、`TerrainCollisionChunk` 和 `TerrainCollisionConfig`，围绕玩家位置维护独立于渲染 LOD 的近景高精度地形代理。
- 玩家与漫游体的地面采样已切换到碰撞代理层，代理未命中时回退到 `WorldMap.sample_height`，保证连续性。
- 碰撞代理分块采用单独半径、细分、每帧构建预算和缓存容量配置，不与渲染分块共享生命周期。
- 已通过 `cargo fmt --all`、`cargo check`、`cargo test`、`cargo clippy --all-targets -- -D warnings` 与 `DAO_AUTO_EXIT_SECONDS=1.0 cargo run` 验证。
