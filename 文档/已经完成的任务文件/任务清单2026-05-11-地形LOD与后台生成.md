# 《道》任务清单：地形 LOD 与后台生成（2026-05-11）

## 阶段目标

1. 建立近处高精度、远处低精度、外围预热三层地形流式策略。
2. 将分块生成主体迁移到后台线程，进一步降低长距离高速移动时的主线程抖动。

## 任务项

### 1. LOD 目标层

- [x] 定义分块 LOD 层级与目标状态。
- [x] 将当前单一可见半径扩展为近处高精度半径、远处低精度半径和预热半径。
- [x] 在目标变化时支持分块升级、降级和预热取消。

### 2. 后台生成流水线

- [x] 建立后台线程生成队列和结果回传通道。
- [x] 后台任务生成 terrain mesh 原始数据和散布布局，主线程只负责资产落地与实体更新。
- [x] 保持缓存、淘汰和 LOD 结果一致，不重复生成相同目标。

### 3. 验证与文档

- [x] 补充测试覆盖 LOD 目标选择、后台任务去重和配置解析。
- [x] 更新设计文档与工具说明。
- [x] 运行格式化、编译、测试、clippy 和启动烟雾验证。

## 完成记录

- 新增 `TerrainLodLevel`、`TerrainChunkKey` 和 `TerrainChunkTarget`，将地形流式从单层分块坐标升级为三层 LOD 目标系统。
- 新增后台 `TerrainGenerationScheduler`，后台线程负责 mesh 与散布布局生成，主线程只做结果集成、缓存复用和显示切换。
- 新增 `high_detail_chunk_radius`、`low_detail_chunk_radius`、`preload_chunk_radius`、`background_generation_budget` 配置项。
- 高精度目标使用原始细分，低精度与预热目标使用降采样分块 mesh；预热目标只缓存不显示。
- 已通过 `scripts/dev-check.ps1` 和 `DAO_AUTO_EXIT_SECONDS=1.0 cargo run` 验证。
