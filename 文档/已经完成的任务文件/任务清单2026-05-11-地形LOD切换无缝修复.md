# 《道》任务清单：地形 LOD 切换无缝修复（2026-05-11）

## 阶段目标

1. 修复玩家靠近已显示地块时出现的瞬时闪烁。
2. 保证同坐标分块在 LOD 升级和降级过程中不中断可见性。
3. 为后续碰撞代理和远景层扩展建立稳定的分块切换规则。

## 任务项

### 1. 可见性切换修复

- [x] 识别同坐标分块在 LOD 切换时被提前卸载的问题。
- [x] 调整可见性判定，在替代分块未真正生成并落地前保留旧分块。
- [x] 在新分块生成完成后同步清理旧 LOD 实体，避免同坐标双层残留。

### 2. 验证

- [x] 补充测试覆盖“替代 LOD 未到位前保留旧块”和“替代块到位后释放旧块”。
- [x] 运行格式化、编译、测试、clippy 和启动烟雾验证。

### 3. 文档

- [x] 更新系统设计文档，记录地形 LOD 无缝交接策略。
- [x] 更新开发工具说明，补充本轮验证结果。

## 完成记录

- `update_visible_chunks` 不再仅按目标 key 集合直接卸载旧分块，而是按“同坐标替代分块是否已落地”决定是否保留旧 LOD。
- 新增同坐标替代规则：目标分块若尚未生成完成，则继续显示旧分块；目标分块一旦生成并集成，立即回收旧 LOD 对应的地形和细节实体。
- 新增两项单元测试，锁定无缝 LOD 交接行为，避免后续流式系统演进时回归。
- 已通过 `scripts/dev-check.ps1` 和 `DAO_AUTO_EXIT_SECONDS=1.0 cargo run` 验证。
